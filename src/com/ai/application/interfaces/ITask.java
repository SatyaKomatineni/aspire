package com.ai.application.interfaces;

/**
 * A tag interface signifying that the implementation is a single task
 * Should be implemented by objects intending to be singletons
 * Maintains no state
 * Parallels a StatelessSessionBean
 * Or a StatelessSessionBean (SLSB) could be seen as a collection of tasks
 */
public interface ITask 
{
} 
